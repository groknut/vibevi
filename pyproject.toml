[project]
name = "vibevi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pdfplumber",
    "pymupdf",
    "python-docx",
    "openpyxl",
    "xlrd",
    "Pillow",
    "pyqt6>=6.11.0",
    "markdown",
    "mutagen",
    "PyYAML",
]

[project.scripts]
start = "main:main"
